<tr>
	<th scope="row" class="text-md-center"><%= order.id %></th>
  <td><%= order.user %></td>
  <td class="text-md-center"><%= order.product_orders.count %></td>
  <td class="text-md-center"><%= order.status %></td>
  <td class="text-md-right"><%= number_to_currency order.product_orders.total %></td>
  <td class="text-md-center"><%= link_to 'Ver Detalle', admin_order_path(order), class: 'btn btn-info'  %> <%= link_to 'Eliminar', admin_order_path(order), method: :delete, class: 'btn btn-danger'  %></td>
</tr>